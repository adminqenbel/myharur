# Public client configuration only. Never put a database password or service-role key here.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_PUBLISHABLE_KEY=<publishable-key>
APP_ENV=development

# Optional public map configuration for the prototype.
MAP_TILE_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png

